# Use this to connect to a db. If a string is not specified it will default to a sqlite db ''sqlite:///snps/refSNP.db'
# See https://docs.sqlalchemy.org/en/13/core/engines.html for sqlalchemy connection string examples
connection_string: ''
